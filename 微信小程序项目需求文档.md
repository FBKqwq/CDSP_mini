# TCM LLM Chart 微信小程序项目需求文档

> 文档版本：V1.0  
> 文档状态：开发基线  
> 编制日期：2026-08-19  
> 项目目录：`C:\Users\zhurunjie\Desktop\CmeAiPlatform\tcm-llm-chart-miniapp`  
> 目标平台：微信小程序  
> 技术路线：uni-app + Vue 3 + TypeScript + Pinia

## 1. 文档目的

本文档用于指导 `llmChart` 微信小程序的产品设计、技术设计、任务拆分、开发实施、联调测试和上线验收。

本项目是一个独立微信小程序项目，不复用 FUOEN 前端工程、页面、组件、路由或状态管理。本期仅建设微信小程序，不包含 FUOEN 右侧面板组件的设计与开发。

## 2. 项目背景

现有系统页面地址为：

`http://127.0.0.1:3006/tcmfronthearken/#/llmChart`

现有页面是面向桌面浏览器的三栏诊疗工作台，主要包括：

- 患者管理；
- 疾病分组和专家选择；
- AI 流式问诊；
- 问诊、初步诊断、专家协作、综合诊断四阶段流程；
- 证候匹配雷达图；
- 诊断报告；
- 报告对比；
- 处方查看；
- 人体脏器参照。

现有实现依赖 Element Plus、ECharts、浏览器 WebSocket、DOM、ShadowRoot、iframe 和 Unity 页面，无法直接运行在微信小程序环境。桌面三栏布局在手机窄屏上还会产生明显的横向溢出。

因此，本项目不是源码复制，而是以现有业务行为和接口数据为基线，在微信小程序约束下重新设计信息架构、交互和运行时实现。

## 3. 目标驱动因果链

### 3.1 最终目标

医务用户能够在手机微信小程序中完成从登录、选择患者、AI 问诊、进入诊断到查看报告和处方的完整业务闭环，并能在弱网、应用前后台切换和流式连接中断后安全恢复。

### 3.2 当前差距

- 现有桌面布局无法在手机端正常使用；
- 现有 UI 和浏览器依赖不受微信小程序运行时支持；
- 现有请求依赖 Vite 本地代理，小程序无法直接复用；
- 当前 WebSocket 客户端是面向浏览器实现，且存在静态单例和跨上下文污染风险；
- 现有报告对比为桌面宽表格，不适合手机阅读；
- 现有人体脏器模块基于 iframe/Unity，本期无法低成本迁移。

### 3.3 根因

现有系统的业务能力、桌面 UI 实现和浏览器基础设施耦合在同一页面中，没有形成可供多端复用的稳定业务协议和会话边界。

### 3.4 技术方向

- 保留业务语义和后端数据，重新实现移动端交互；
- 建立独立 uni-app 工程；
- 通过统一 HTTPS/WSS 网关隔离旧服务路径和鉴权差异；
- 用会话状态机管理 AI 问诊生命周期；
- 用 `sessionVersion` 隔离患者、疾病和专家切换前后的异步数据；
- 用小程序 Canvas 重建雷达图；
- 将报告对比改造成移动端纵向对比；
- 本期明确排除人体脏器参照功能。

## 4. 项目范围

### 4.1 本期范围

1. 独立微信小程序工程搭建；
2. 登录、退出和身份失效处理；
3. 患者查询、新增、删除和选择；
4. 疾病分组和专家选择；
5. AI 问诊实例创建和 WSS 流式通信；
6. 问诊思考过程、执行步骤、工具调用和正文输出展示；
7. 问诊记录保存、恢复和继续；
8. 进入诊断及四阶段诊疗进度；
9. 证候匹配雷达图；
10. 诊断报告；
11. 报告对比；
12. 处方查看；
13. 弱网、重连、前后台切换和异常恢复；
14. 隐私保护、日志脱敏和上线检查。

### 4.2 本期不包含

- FUOEN 右侧面板或完整页面集成；
- FUOEN 前端代码复用；
- 人体脏器参照、Unity 或 3D 模型；
- 医生账号注册、找回密码和账号审批；
- 后端诊疗算法重写；
- 离线问诊；
- 面向患者的自助问诊模式；
- 支付、消息订阅和客服能力。

## 5. 用户与使用场景

### 5.1 目标用户

本期目标用户为已获得系统账号的医务人员或授权业务人员。

### 5.2 核心场景

1. 医务用户登录小程序；
2. 搜索并选择已有患者，或新增患者；
3. 选择疾病分组和参与诊疗的专家；
4. 与 AI 进行流式问诊；
5. 确认信息充分后进入诊断；
6. 查看诊疗阶段、证候匹配和诊断结论；
7. 查看历史报告、进行报告对比并查看处方；
8. 在网络中断或退出小程序后恢复当前工作上下文。

## 6. 产品信息架构

```text
登录页
  └─ 诊疗工作台
      ├─ 顶部诊疗上下文
      │   ├─ 患者选择
      │   ├─ 疾病选择
      │   └─ 专家选择
      ├─ 问诊
      │   ├─ AI 对话
      │   ├─ 思考与执行步骤
      │   └─ 进入诊断
      ├─ 诊疗
      │   ├─ 四阶段进度
      │   ├─ 证候雷达图
      │   └─ 诊断结论
      └─ 报告
          ├─ 诊断报告
          ├─ 报告对比
          └─ 处方
```

诊疗工作台使用单页面承载三个内部功能入口，不使用三个互相独立的原生 Tab 页面，以避免页面切换导致 WebSocket 和问诊上下文反复销毁。

## 7. 总体交互原则

### 7.1 设备适配

- 主要设计基准：390 × 844；
- 手机支持宽度：320～430px；
- 平板端采用自适应布局，但不恢复桌面三栏结构；
- 页面不得出现非业务需要的横向滚动；
- 底部操作区必须避开安全区域。

### 7.2 工作台结构

- 顶部固定显示当前患者、疾病、专家和诊疗阶段；
- 点击患者、疾病或专家时打开底部选择面板；
- 底部提供“问诊”“诊疗”“报告”三个入口；
- AI 正在输出时切换功能入口，不得中断当前连接；
- 切换患者、疾病或专家属于诊疗上下文变更，必须二次确认并创建新的会话版本。

### 7.3 通用状态

每个涉及远程数据的模块必须覆盖：

- 首次加载；
- 空数据；
- 加载成功；
- 加载失败；
- 无权限；
- 登录失效；
- 网络不可用；
- 重试中；
- 服务端业务错误。

不得只通过 Toast 表达需要用户决策或会导致数据丢失的错误。

## 8. 功能需求

### 8.1 身份认证

#### FR-AUTH-001 登录

- 用户输入账号和密码后发起登录；
- 账号和密码均为必填项；
- 提交期间按钮进入加载状态，禁止重复提交；
- 登录成功后保存访问令牌和必要的用户摘要，进入诊疗工作台；
- 登录失败时展示服务端可读错误，不清空账号；
- 密码不得写入日志或持久化缓存。

#### FR-AUTH-002 身份保持

- 小程序重新打开时读取本地令牌；
- 令牌有效时进入工作台；
- 令牌无效或过期时清除本地会话并返回登录页；
- REST 与 WSS 必须使用同一身份体系。

#### FR-AUTH-003 退出

- 用户可主动退出；
- 退出前关闭活动 Socket；
- 清除令牌、当前患者标识和诊疗上下文；
- 不在本地长期保存完整患者资料、聊天正文或诊断报告。

### 8.2 诊疗上下文

#### FR-CTX-001 上下文展示

工作台顶部持续展示：

- 当前患者姓名或患者编号；
- 当前疾病；
- 当前专家；
- 当前诊疗阶段。

缺少任一必需上下文时，问诊输入区应禁用，并明确提示缺少的项目。

#### FR-CTX-002 上下文切换

- 用户可以切换患者、疾病和专家；
- 存在流式问诊或未完成诊疗时必须二次确认；
- 用户确认切换后，关闭旧 Socket 并递增 `sessionVersion`；
- 旧请求、旧定时器和旧 Socket 事件不得再更新当前页面；
- 切换完成后加载新上下文的咨询记录和诊断数据。

### 8.3 患者管理

#### FR-PAT-001 患者列表

- 支持加载患者列表；
- 支持按姓名或患者编号搜索；
- 显示当前选中状态；
- 无患者时引导新增患者；
- 列表加载失败时允许重试。

#### FR-PAT-002 新增患者

- 使用独立表单或底部弹层新增患者；
- 字段以现有患者接口的有效字段为准；
- 必填项、格式和长度在客户端预校验；
- 提交成功后刷新列表并默认选中新患者；
- 提交失败时保留用户已填写内容。

#### FR-PAT-003 删除患者

- 删除前展示患者身份和不可逆提示；
- 当前患者存在活动问诊时，必须先结束或放弃当前会话；
- 删除成功后清空相应上下文并刷新列表；
- 删除失败时保持原选中状态。

### 8.4 疾病与专家选择

#### FR-CONFIG-001 疾病分组

- 按现有服务返回的疾病分组展示；
- 支持查看疾病名称和必要说明；
- 选择结果写入当前诊疗上下文。

#### FR-CONFIG-002 专家选择

- 展示可选专家名称及必要摘要；
- 支持单选；
- 专家不可用时显示原因并禁止选择；
- 选择变更遵循上下文切换规则。

### 8.5 AI 问诊

#### FR-CHAT-001 问诊初始化

- 患者、疾病和专家齐备后允许开始问诊；
- 客户端通过 REST 创建 AI 对话实例；
- 获得 `instanceId` 后建立 WSS 连接；
- Socket 连接成功前不得假装问诊已经开始；
- 初始化失败时提供重试入口。

#### FR-CHAT-002 消息发送

- 支持多行文本输入；
- 空白文本不可发送；
- 发送后立即生成用户消息的本地待确认状态；
- 服务端确认后更新为已发送；
- 失败时标记失败并支持重新发送；
- AI 正在输出时，默认禁止重复发送新的用户消息。

#### FR-CHAT-003 流式输出

客户端至少支持以下事件：

| 事件 | 展示或处理要求 |
|---|---|
| `thinking` | 更新可折叠的思考摘要，不展示内部敏感提示词 |
| `output_step` | 更新当前执行步骤和进度 |
| `tool_call` | 显示正在调用的业务工具摘要 |
| `tool_result` | 更新工具执行结果状态 |
| `output` | 逐段追加 AI 正文 |
| `output_end` | 结束本轮输出并触发记录保存 |
| `chat_message` | 处理服务端补发或标准消息 |
| `stats` | 仅用于必要状态或诊断日志，不直接显示技术指标 |
| `error` | 结束当前输出并进入可恢复错误状态 |
| `system` | 处理心跳、会话状态或系统通知 |

#### FR-CHAT-004 消息展示

- 区分用户、AI 和系统消息；
- 支持安全的 Markdown 子集；
- 不允许将服务端文本作为原始 HTML 直接注入；
- 长内容支持复制；
- 新内容到达时，仅在用户处于列表底部附近时自动滚动；
- 用户查看历史消息时，不得强制拉回底部，应显示“回到最新”入口。

#### FR-CHAT-005 思考过程

- 默认显示简短状态，如“正在分析病情”；
- 详细执行步骤可折叠查看；
- 不展示模型系统提示词、密钥、内部接口参数或完整工具原始响应；
- 工具失败时展示可理解的业务提示。

#### FR-CHAT-006 进入诊断

- 服务端达到可诊断条件时显示“进入诊断”；
- 用户点击后必须二次确认；
- 确认后提交当前问诊资料并进入诊断阶段；
- 提交期间禁止重复操作；
- 提交失败时保留当前问诊内容并允许重试。

#### FR-CHAT-007 记录恢复

- 进入某患者诊疗上下文时查询已有咨询记录；
- 恢复历史用户消息、AI 消息和当前诊疗阶段；
- 若服务端会话不可继续，应保留历史只读记录并创建新实例；
- 不以本地缓存作为医疗记录的权威来源。

### 8.6 WebSocket 生命周期

#### FR-WS-001 单会话连接

- 每个活动诊疗会话最多存在一个 Socket；
- Socket 管理器必须是实例级对象，不使用跨患者共享的静态单例；
- 切换上下文、退出登录或页面销毁时主动关闭连接。

#### FR-WS-002 重连

- 非用户主动关闭时允许自动重连；
- 使用有上限的退避策略，默认最多尝试 5 次；
- 重连期间展示状态；
- 重连前确认 `sessionVersion` 和 `instanceId` 仍属于当前上下文；
- 达到上限后进入手动重试状态，不无限循环。

#### FR-WS-003 前后台切换

- 小程序进入后台时记录当前连接和输出状态；
- 根据微信运行时约束暂停或关闭 Socket；
- 返回前台后先校验身份和会话，再决定是否重连；
- 恢复时通过服务端记录弥补可能丢失的消息，不只依赖本地增量。

### 8.7 诊疗进度与诊断

#### FR-DIAG-001 四阶段进度

展示以下阶段：

1. 问诊；
2. 初步诊断；
3. 专家协作；
4. 综合诊断。

每一阶段支持未开始、进行中、已完成和失败状态。阶段状态以服务端数据为准。

#### FR-DIAG-002 证候雷达图

- 使用小程序 Canvas 绘制，不引入依赖 DOM 的 ECharts 运行时；
- 维度名称和数值来自服务端；
- 显示数值范围、图例和当前证候；
- 数据缺失时显示空状态，不补造数据；
- 维度过多时采用可读性降级策略，例如缩短标签或展示明细列表。

#### FR-DIAG-003 诊断结论

- 展示主要诊断、证候、辨证依据和必要建议；
- 保留服务端字段语义，不在客户端推导医疗结论；
- 长内容分区展示；
- 数据更新时明确显示更新时间。

### 8.8 报告与处方

#### FR-REPORT-001 报告列表

- 展示当前患者可访问的诊断报告；
- 显示报告时间、疾病、阶段和状态；
- 支持刷新；
- 无报告时给出业务说明。

#### FR-REPORT-002 报告详情

- 分区展示患者摘要、诊断结论、证候、辨证依据和诊疗建议；
- 不在页面中暴露无关患者敏感字段；
- 报告内容以服务端记录为权威来源。

#### FR-REPORT-003 报告对比

- 用户选择两份可比较的报告；
- 调用服务端对比接口；
- 按诊断维度使用纵向卡片或折叠面板展示；
- 每个维度明确标识报告 A、报告 B 和差异摘要；
- 不使用需要横向拖动的桌面宽表格；
- 对比失败不影响单份报告查看。

#### FR-RX-001 处方

- 展示处方名称、药物、剂量、用法和服务端提供的注意事项；
- 字段缺失时不自行补全；
- 剂量和单位必须原样保留；
- 本期只读，不提供客户端修改和开立处方能力。

## 9. 页面需求

### 9.1 登录页

主要元素：

- 系统标识；
- 账号输入框；
- 密码输入框；
- 登录按钮；
- 错误信息；
- 隐私说明入口。

交互要求：键盘弹出后登录按钮仍可操作；密码默认隐藏；登录期间防止重复提交。

### 9.2 诊疗工作台——问诊

主要元素：

- 顶部诊疗上下文；
- 连接状态；
- 消息列表；
- AI 执行步骤；
- 输入区；
- 进入诊断操作；
- 底部功能入口。

### 9.3 诊疗工作台——诊疗

主要元素：

- 四阶段进度；
- 当前诊断摘要；
- 证候雷达图；
- 诊断依据列表；
- 更新时间。

### 9.4 诊疗工作台——报告

主要元素：

- 报告列表；
- 报告详情入口；
- 报告对比入口；
- 处方入口。

### 9.5 选择面板

患者、疾病和专家统一采用底部弹层交互，但各自保持独立数据和筛选逻辑。面板支持标题、搜索或筛选、列表、当前选中状态、确认和取消。

### 9.6 首批 UI 设计交付物

编码前必须完成并评审以下高保真效果图：

1. 登录页；
2. AI 问诊默认状态；
3. AI 流式输出与思考步骤状态；
4. 诊疗概览；
5. 报告与处方；
6. 患者、疾病和专家选择面板；
7. 网络异常、重连和会话失效状态。

UI 评审通过后才能进入完整页面开发，避免结构性返工。

## 10. 会话状态模型

### 10.1 问诊状态机

```text
IDLE
  → CREATING_INSTANCE
  → CONNECTING
  → READY
  → SENDING
  → STREAMING
  → COMPLETED

异常分支：
CONNECTING / SENDING / STREAMING
  → INTERRUPTED
  → RECONNECTING
  → READY 或 STREAMING
  → FAILED
```

### 10.2 状态约束

- `CREATING_INSTANCE` 阶段不得创建第二个实例；
- `STREAMING` 阶段不得重复发送用户输入；
- `COMPLETED` 表示当前轮次完成，不代表整个诊疗结束；
- `FAILED` 必须保留可恢复信息；
- 只有当前 `sessionVersion` 的事件可以修改界面状态；
- 服务端诊疗记录是权威数据，本地状态是视图缓存。

### 10.3 核心数据对象

```ts
interface AuthSession {
  accessToken: string
  expiresAt: number
  user: UserSummary
}

interface ConsultationContext {
  sessionVersion: number
  patientId: string
  diseaseGroupId: string
  doctorId: string
  consultationId?: string
  instanceId?: string
  stage: 'consultation' | 'preliminary' | 'collaboration' | 'comprehensive'
}

interface ChatMessage {
  id: string
  role: 'user' | 'assistant' | 'system'
  content: string
  status: 'pending' | 'streaming' | 'sent' | 'failed'
  createdAt: number
}
```

字段名称可在接口契约阶段调整，但上下文版本、实例标识和消息状态不可缺失。

## 11. 接口与网关需求

### 11.1 原则

- 小程序不得依赖 Vite 本地代理；
- 生产环境必须使用微信后台配置的 HTTPS/WSS 合法域名；
- 前端只调用版本化网关接口，不直接拼接多个旧服务地址；
- REST 和 WSS 使用统一 Token；
- 网关统一转换错误码和旧接口字段；
- 前端不得承担旧系统密码兼容哈希逻辑，兼容逻辑放在网关或认证服务边界。

### 11.2 建议外部接口

```text
POST   /api/v1/auth/login
POST   /api/v1/auth/logout
GET    /api/v1/auth/me

GET    /api/v1/llm-chart/patients
POST   /api/v1/llm-chart/patients
DELETE /api/v1/llm-chart/patients/{patientId}

GET    /api/v1/llm-chart/disease-groups
GET    /api/v1/llm-chart/doctors

GET    /api/v1/llm-chart/consultations
POST   /api/v1/llm-chart/consultations
PUT    /api/v1/llm-chart/consultations/{consultationId}

GET    /api/v1/llm-chart/records
POST   /api/v1/llm-chart/records

POST   /api/v1/llm-chart/chat/instances
POST   /api/v1/llm-chart/diagnoses
POST   /api/v1/llm-chart/reports/compare

WSS    /ws/v1/llm-chart/stream?instanceId={instanceId}
```

最终路径以网关接口评审结果为准，但小程序内部必须通过 `llm-chart-api.ts` 统一访问，页面不得直接调用 URL。

### 11.3 现有服务能力映射基线

| 现有能力 | 现有接口基线 |
|---|---|
| 患者查询 | `GET /agent/service/api/patient/select` |
| 患者新增 | `POST /agent/service/api/patient/create` |
| 患者删除 | `POST /agent/service/api/patient/delete` |
| 疾病分组 | `GET /agent/service/api/disease-group/select` |
| 患者诊疗初始化 | `POST /agent/service/api/patient/inite` |
| 咨询查询 | `GET /agent/service/api/consultation/select` |
| 咨询更新 | `POST /agent/service/api/consultation/update` |
| 配置查询 | `GET /agent/service/api/config/select` |
| 记录查询 | `GET /agent/service/api/records/select` |
| 记录更新 | `POST /agent/service/api/records/update` |
| 咨询删除 | `POST /agent/service/api/consultation/delete` |
| 诊断更新 | `POST /agent/service/api/diagnosis/update` |
| 报告对比 | `POST /tcmfronthearken/service/agent/service/api/compare/diagnosis` |
| AI 实例创建 | `POST /das-pm/pm/ai-agent/chat/instance` |
| AI 流式连接 | `WS /das-pm/ws/llm/stream?instanceId=...` |

该表用于联调追踪，不代表小程序应直接访问这些旧路径。

### 11.4 统一响应与错误

建议响应格式：

```json
{
  "success": true,
  "code": "OK",
  "message": "",
  "data": {}
}
```

至少统一以下错误类型：

- `AUTH_INVALID`：账号或密码错误；
- `TOKEN_EXPIRED`：登录过期；
- `FORBIDDEN`：无访问权限；
- `CONTEXT_INVALID`：患者、疾病或专家上下文无效；
- `INSTANCE_INVALID`：AI 实例无效；
- `STREAM_INTERRUPTED`：流式连接中断；
- `SERVICE_UNAVAILABLE`：服务暂不可用；
- `VALIDATION_ERROR`：输入校验失败。

## 12. 工程与代码要求

### 12.1 技术栈

- uni-app 官方 Vue 3 技术路线；
- TypeScript；
- Pinia；
- Vite；
- `uni.request`；
- `uni.connectSocket`；
- 小程序 Canvas；
- 轻量、安全的 Markdown 节点渲染方案。

### 12.2 禁止依赖

- Element Plus；
- 依赖 DOM 的 ECharts 浏览器版本；
- `window`、`document`、`navigator`、ShadowRoot；
- iframe；
- 浏览器原生 `WebSocket`；
- 在模块顶层共享患者会话的静态 Socket 单例；
- 将生产地址硬编码在页面中。

### 12.3 建议目录

```text
tcm-llm-chart-miniapp/
├─ src/
│  ├─ pages/
│  │  ├─ login/
│  │  ├─ workbench/
│  │  ├─ patient/
│  │  └─ report/
│  ├─ components/
│  │  ├─ context-selector/
│  │  ├─ consultation/
│  │  ├─ diagnosis/
│  │  ├─ report/
│  │  └─ common/
│  ├─ stores/
│  ├─ services/
│  ├─ domain/
│  ├─ types/
│  ├─ config/
│  └─ utils/
├─ tests/
├─ manifest.json
├─ pages.json
└─ package.json
```

### 12.4 状态所有权

- `authStore`：只管理身份；
- `patientStore`：管理患者列表和当前患者摘要；
- `consultationStore`：管理诊疗上下文、消息和诊疗状态；
- `SocketSession`：管理单个 AI 实例的连接；
- 页面负责展示和派发用户意图，不直接维护重复的业务状态。

## 13. 非功能需求

### 13.1 安全与隐私

- 所有正式接口使用 HTTPS/WSS；
- Token 不放在 URL 查询参数中，`instanceId` 除外但不得包含身份凭据；
- 密码、Token、患者姓名、聊天正文和诊断内容不得写入普通运行日志；
- 错误上报默认进行患者信息脱敏；
- 本地仅持久化 Token、用户摘要和必要上下文标识；
- 不持久化完整病历、完整聊天记录、报告正文和处方；
- Markdown 内容必须经过安全解析；
- 退出登录时清除本地身份和业务缓存。

### 13.2 可靠性

- 同一上下文不重复创建 AI 实例；
- REST 请求具备超时和明确失败状态；
- 非幂等操作不得自动无条件重试；
- Socket 自动重连有上限；
- 从后台恢复时以服务端记录校正本地消息；
- 任何异步回调写状态前校验 `sessionVersion`。

### 13.3 性能

- 首屏优先渲染页面骨架，再加载非关键业务数据；
- 长消息列表使用分页、窗口化或分段加载策略；
- 流式文本更新需要合并刷新，避免每个字符触发全页面重绘；
- 雷达图只在数据或尺寸变化时重绘；
- 图片和图标资源需要压缩；
- 构建产物必须满足实施时微信小程序最新包体限制。

### 13.4 可用性

- 关键点击区域满足移动端触控需求；
- 文字与背景保持足够对比度；
- 颜色不得作为阶段和错误状态的唯一识别方式；
- 医疗术语不随意缩写；
- 操作结果必须有可感知反馈。

### 13.5 兼容性

- 微信开发者工具当前稳定版本；
- 主流 Android 微信环境；
- 主流 iOS 微信环境；
- 手机竖屏为主；
- 平板竖屏和横屏完成基本适配。

## 14. 埋点与运行诊断

在不记录医疗正文的前提下，至少采集以下技术事件：

- 登录成功/失败类型；
- 患者上下文初始化成功/失败；
- AI 实例创建成功/失败；
- Socket 建连耗时；
- Socket 非主动断开；
- 自动重连次数和最终结果；
- 首段 AI 输出耗时；
- 报告加载和对比失败类型；
- 登录失效次数；
- 未捕获异常。

日志使用匿名会话标识，不使用患者姓名作为检索键。

## 15. 验收标准

### AC-001 独立工程

- 项目可在目标目录独立安装依赖、构建和运行；
- 不依赖 FUOEN 前端源码或运行时；
- 不依赖原项目 Vite 代理才能启动。

### AC-002 登录闭环

- 正确账号能够登录；
- 错误账号得到明确提示；
- Token 失效后返回登录页；
- 退出后无法继续访问患者数据。

### AC-003 患者闭环

- 可以查询、搜索、新增、选择和删除患者；
- 删除需要确认；
- 切换患者后不显示旧患者的消息、诊断或报告。

### AC-004 AI 问诊闭环

- 能创建 AI 实例并建立 WSS；
- 能发送消息并接收流式输出；
- 能显示必要思考状态和执行步骤；
- `output_end` 后记录被保存；
- 中断后能够自动或手动恢复。

### AC-005 诊断闭环

- 满足条件时能够进入诊断；
- 展示四阶段进度；
- 展示服务端返回的诊断结论和证候数据；
- 雷达图与明细数据一致。

### AC-006 报告闭环

- 能查看报告列表和详情；
- 能选择两份报告进行纵向对比；
- 能查看只读处方；
- 缺失字段不被客户端臆造。

### AC-007 移动端体验

- 320～430px 宽度无页面级横向溢出；
- 键盘弹出时输入和发送仍可操作；
- 底部内容不被安全区域遮挡；
- 长报告可连续阅读。

### AC-008 上下文隔离

- 流式输出期间切换患者并确认后，旧 Socket 被关闭；
- 即使旧连接随后返回消息，也不得写入新患者页面；
- 快速连续切换疾病或专家不会出现数据串线。

### AC-009 弱网恢复

- 断网时展示明确状态；
- 恢复网络后按策略重连；
- 达到重连上限后停止自动尝试；
- 返回前台后能够从服务端校正消息。

### AC-010 安全

- 日志中不存在密码、Token 和完整医疗正文；
- 本地缓存中不存在完整病历和处方；
- 所有生产通信通过 HTTPS/WSS；
- 原始 HTML 无法通过聊天内容执行。

## 16. 测试要求

### 16.1 单元测试

- 问诊状态机合法和非法迁移；
- `sessionVersion` 过滤；
- WebSocket 事件解析；
- 重连次数和退避；
- Markdown 安全节点转换；
- 接口错误码映射；
- 报告对比数据转换。

### 16.2 集成测试

- 登录—患者选择—实例创建—WSS 首条输出；
- 多轮问诊—记录保存—重新进入恢复；
- 问诊—进入诊断—报告生成；
- 报告选择—对比接口—结果展示；
- Token 过期下的 REST 和 WSS 行为一致性。

### 16.3 真机专项测试

- iOS/Android 键盘与输入框；
- 前后台切换；
- Wi-Fi/移动网络切换；
- 弱网、断网和恢复；
- 长消息和长报告；
- 小屏和大屏适配；
- Canvas 在不同像素密度下的清晰度。

## 17. 开发阶段与交付物

### 阶段 0：UI 与接口基线

交付物：

- 高保真效果图；
- 页面状态说明；
- REST/WSS 接口契约；
- 字段字典；
- 错误码表；
- 微信合法域名和证书方案。

退出条件：UI 结构和“登录—患者—AI 实例—WSS”接口链路评审通过。

### 阶段 1：黄金纵向切片

交付物：

- 独立 uni-app 工程；
- 登录；
- 患者选择；
- AI 实例创建；
- WSS 建连；
- 首轮流式消息展示。

退出条件：在微信开发者工具和至少一台真机完成真实接口闭环。

### 阶段 2：患者与完整问诊

交付物：

- 患者增删查选；
- 疾病和专家选择；
- 多轮问诊；
- 思考和工具步骤；
- 消息保存与恢复；
- 中断重连和前后台恢复。

### 阶段 3：诊疗与报告

交付物：

- 进入诊断；
- 四阶段进度；
- 雷达图；
- 诊断报告；
- 报告对比；
- 处方详情。

### 阶段 4：质量与发布

交付物：

- 自动化测试结果；
- 真机测试报告；
- 隐私与日志检查报告；
- 体验版；
- 发布配置和回滚说明；
- 运维监控说明。

## 18. 依赖与风险

| 风险或依赖 | 影响 | 处理要求 |
|---|---|---|
| HTTPS/WSS 网关未就绪 | 无法真机联调 | 阶段 0 必须确定域名、证书和负责人 |
| 旧登录服务不签发 Token | REST/WSS 无法统一鉴权 | 在认证网关签发并校验小程序会话 Token |
| 旧服务字段不稳定 | 页面出现大量兼容判断 | 在网关或 API 适配层统一 DTO |
| WebSocket 事件缺少顺序或重放约定 | 恢复时丢消息或重复消息 | 接口契约明确事件 ID、轮次和完成标识 |
| 患者切换与旧异步回调并发 | 医疗数据串线 | 强制 `sessionVersion` 校验和旧连接关闭 |
| 长文本高频流式更新 | 页面卡顿 | 合并刷新并限制重渲染范围 |
| 3D 人体模块无法移植 | 范围膨胀 | 本期明确不包含，后续单独立项 |
| 医疗正文进入日志或缓存 | 隐私风险 | 默认脱敏、最小持久化、发布前专项审计 |

## 19. 开发启动前置条件

满足以下条件后才进入完整开发：

1. 微信小程序 AppID 和主体信息可用；
2. 开发、测试和生产 HTTPS/WSS 域名确定；
3. 网关负责人和部署环境确定；
4. 登录和 Token 契约确定；
5. 患者、咨询、诊断、报告和处方字段字典冻结；
6. WebSocket 事件样例和顺序规则确定；
7. 高保真效果图通过评审；
8. 测试账号和脱敏测试患者可用。

## 20. 完成定义（Definition of Done）

本项目只有在以下条件全部满足时才视为本期完成：

- 本文档范围内的功能需求全部通过验收；
- 黄金链路在真实后端和真机环境跑通；
- 关键状态机和上下文隔离具有自动化测试；
- 弱网、前后台切换和登录失效测试通过；
- 无阻塞级和严重级缺陷；
- 日志、缓存和通信安全检查通过；
- UI 在目标手机尺寸无结构性溢出；
- 小程序体验版通过业务评审；
- 发布配置、监控和回滚说明齐全；
- 人体脏器参照和 FUOEN 面板未被误纳入本期范围。

## 21. 后续迭代候选项

以下内容不属于当前承诺，仅作为后续独立评估项：

- 人体脏器或 3D 可视化的小程序替代方案；
- 报告导出与分享；
- 消息订阅；
- 多端会话同步；
- 更细粒度的医生权限；
- 平板专用双栏工作模式；
- 患者端协同能力。

任何候选项进入开发前，必须重新完成目标、根因、技术边界和验收标准评审，不得直接加入当前版本。
